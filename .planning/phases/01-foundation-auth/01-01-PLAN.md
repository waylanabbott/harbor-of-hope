---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/HarborOfHope.API/HarborOfHope.API.csproj
  - backend/HarborOfHope.API/Program.cs
  - backend/HarborOfHope.API/appsettings.json
  - backend/HarborOfHope.API/appsettings.Development.json
  - backend/HarborOfHope.API/Data/AppDbContext.cs
  - backend/HarborOfHope.API/Data/Entities/Safehouse.cs
  - backend/HarborOfHope.API/Data/Entities/Resident.cs
  - backend/HarborOfHope.API/Data/Entities/Supporter.cs
  - backend/HarborOfHope.API/Data/Entities/Donation.cs
  - backend/HarborOfHope.API/Data/Entities/DonationAllocation.cs
  - backend/HarborOfHope.API/Data/Entities/InKindDonationItem.cs
  - backend/HarborOfHope.API/Data/Entities/EducationRecord.cs
  - backend/HarborOfHope.API/Data/Entities/HealthWellbeingRecord.cs
  - backend/HarborOfHope.API/Data/Entities/ProcessRecording.cs
  - backend/HarborOfHope.API/Data/Entities/HomeVisitation.cs
  - backend/HarborOfHope.API/Data/Entities/IncidentReport.cs
  - backend/HarborOfHope.API/Data/Entities/InterventionPlan.cs
  - backend/HarborOfHope.API/Data/Entities/Partner.cs
  - backend/HarborOfHope.API/Data/Entities/PartnerAssignment.cs
  - backend/HarborOfHope.API/Data/Entities/SocialMediaPost.cs
  - backend/HarborOfHope.API/Data/Entities/SafehouseMonthlyMetric.cs
  - backend/HarborOfHope.API/Data/Entities/PublicImpactSnapshot.cs
  - backend/HarborOfHope.API/Data/SeedData.cs
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/theme.ts
  - frontend/index.html
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "PostgreSQL database has all 17 tables created via EF Core migrations"
    - "All 17 CSV files (~8,100 rows) are seeded into the database on first startup"
    - "Indexes exist on resident_id, supporter_id, safehouse_id, session_date, donation_date, case_status, current_risk_level"
    - "Backend API starts and responds on https://localhost:5001/swagger"
    - "Frontend dev server starts and proxies /api to backend"
  artifacts:
    - path: "backend/HarborOfHope.API/Data/AppDbContext.cs"
      provides: "17-table domain DbContext with indexes"
      contains: "DbSet<Safehouse>"
    - path: "backend/HarborOfHope.API/Data/SeedData.cs"
      provides: "CSV seeding for all 17 tables in dependency order"
      contains: "CsvReader"
    - path: "backend/HarborOfHope.API/Data/Entities/Resident.cs"
      provides: "Resident entity with all CSV columns mapped"
      contains: "resident_id"
    - path: "frontend/vite.config.ts"
      provides: "Vite proxy for /api to .NET backend"
      contains: "proxy"
    - path: "frontend/src/theme.ts"
      provides: "MUI theme with coral/cream/Nunito palette"
      contains: "#E8735A"
  key_links:
    - from: "backend/HarborOfHope.API/Program.cs"
      to: "backend/HarborOfHope.API/Data/AppDbContext.cs"
      via: "UseNpgsql DI registration"
      pattern: "UseNpgsql"
    - from: "backend/HarborOfHope.API/Program.cs"
      to: "backend/HarborOfHope.API/Data/SeedData.cs"
      via: "SeedData.SeedAsync call at startup"
      pattern: "SeedAsync"
    - from: "frontend/vite.config.ts"
      to: "backend"
      via: "proxy /api to https://localhost:5001"
      pattern: "target.*localhost.*5001"
---

<objective>
Scaffold the .NET 10 backend and React/TypeScript frontend projects, create all 17 EF Core entity classes from CSV column analysis, configure AppDbContext with indexes, implement CSV seeding via CsvHelper, and set up the frontend with MUI theme and Vite proxy.

Purpose: Every subsequent plan depends on these two projects existing, the database schema being defined, and seed data being loadable. This is the absolute foundation.
Output: Working backend API at localhost:5001 with Swagger, working frontend dev server at localhost:3000, PostgreSQL with 17 tables and ~8,100 seeded rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold backend project, create all 17 entity classes, AppDbContext with indexes, and CSV seeder</name>
  <files>
    backend/HarborOfHope.API/HarborOfHope.API.csproj
    backend/HarborOfHope.API/Program.cs
    backend/HarborOfHope.API/appsettings.json
    backend/HarborOfHope.API/appsettings.Development.json
    backend/HarborOfHope.API/Data/AppDbContext.cs
    backend/HarborOfHope.API/Data/SeedData.cs
    backend/HarborOfHope.API/Data/Entities/Safehouse.cs
    backend/HarborOfHope.API/Data/Entities/Resident.cs
    backend/HarborOfHope.API/Data/Entities/Supporter.cs
    backend/HarborOfHope.API/Data/Entities/Donation.cs
    backend/HarborOfHope.API/Data/Entities/DonationAllocation.cs
    backend/HarborOfHope.API/Data/Entities/InKindDonationItem.cs
    backend/HarborOfHope.API/Data/Entities/EducationRecord.cs
    backend/HarborOfHope.API/Data/Entities/HealthWellbeingRecord.cs
    backend/HarborOfHope.API/Data/Entities/ProcessRecording.cs
    backend/HarborOfHope.API/Data/Entities/HomeVisitation.cs
    backend/HarborOfHope.API/Data/Entities/IncidentReport.cs
    backend/HarborOfHope.API/Data/Entities/InterventionPlan.cs
    backend/HarborOfHope.API/Data/Entities/Partner.cs
    backend/HarborOfHope.API/Data/Entities/PartnerAssignment.cs
    backend/HarborOfHope.API/Data/Entities/SocialMediaPost.cs
    backend/HarborOfHope.API/Data/Entities/SafehouseMonthlyMetric.cs
    backend/HarborOfHope.API/Data/Entities/PublicImpactSnapshot.cs
  </files>
  <read_first>
    /Users/waylansmac/INTEX 2026/.planning/phases/01-foundation-auth/01-RESEARCH.md
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/residents.csv (first 3 lines for column mapping)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/safehouses.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/supporters.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/donations.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/donation_allocations.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/process_recordings.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/home_visitations.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/education_records.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/health_wellbeing_records.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/incident_reports.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/intervention_plans.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/partners.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/partner_assignments.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/social_media_posts.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/safehouse_monthly_metrics.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/public_impact_snapshots.csv (first 3 lines)
    /Users/waylansmac/INTEX 2026/data/lighthouse_csv_v7/in_kind_donation_items.csv (first 3 lines)
  </read_first>
  <action>
    **Step 1: Scaffold the .NET backend**
    ```bash
    cd "/Users/waylansmac/INTEX 2026"
    mkdir -p backend
    cd backend
    dotnet new webapi -n HarborOfHope.API --framework net10.0
    cd HarborOfHope.API
    dotnet add package Npgsql.EntityFrameworkCore.PostgreSQL --version 10.0.1
    dotnet add package Microsoft.EntityFrameworkCore.Design
    dotnet add package CsvHelper --version 33.1.0
    dotnet add package Swashbuckle.AspNetCore --version 6.6.2
    dotnet tool install --global dotnet-ef 2>/dev/null || true
    ```

    **Step 2: Create all 17 entity classes in `Data/Entities/`**

    Map EVERY column from each CSV header to a C# property. Use PascalCase property names with `[Column("snake_case")]` attributes matching exact CSV column names. Key type mappings from RESEARCH.md:
    - Primary keys: `[Key]` attribute, int type (e.g., `resident_id`, `safehouse_id`)
    - Foreign keys: int type with `[ForeignKey]` navigation property
    - Dates: `DateTime?` (nullable since some CSVs have empty dates)
    - Booleans: `bool` for `True`/`False` columns (sub_cat_*, is_recurring, etc.)
    - Currency: `decimal` for amount fields (donations, budget)
    - Age fields on residents: `string` (e.g., "15 Years 9 months" -- NOT numeric)
    - `metric_payload_json` on PublicImpactSnapshot: `string` (store as text)
    - `partner_assignments.safehouse_id`: int (CSV has "8.0" format -- CsvHelper ClassMap will handle)
    - All text fields: `string?` (nullable)

    Entity files to create (17 total):
    1. `Safehouse.cs` -- PK: safehouse_id, columns: safehouse_code, name, region, city, province, country, open_date, status, capacity_girls, capacity_staff, current_occupancy, notes
    2. `Resident.cs` -- PK: resident_id, FK: safehouse_id, 47 columns total including 8 sub_cat_* booleans, all date fields as DateTime?, age fields as string
    3. `Supporter.cs` -- PK: supporter_id, columns: supporter_type, display_name, organization_name, first_name, last_name, relationship_type, region, country, email, phone, status, created_at, first_donation_date, acquisition_channel
    4. `Donation.cs` -- PK: donation_id, FK: supporter_id, columns: donation_type, donation_date, is_recurring, campaign_name, channel_source, currency_code, amount (decimal), estimated_value (decimal?), impact_unit, notes, referral_post_id
    5. `DonationAllocation.cs` -- PK: allocation_id, FKs: donation_id, safehouse_id, columns: program_area, amount_allocated (decimal), allocation_date, allocation_notes
    6. `InKindDonationItem.cs` -- PK: item_id, FK: donation_id, columns: item_name, item_category, quantity (int), unit_of_measure, estimated_unit_value (decimal?), intended_use, received_condition
    7. `EducationRecord.cs` -- PK: education_record_id, FK: resident_id, columns: record_date, education_level, school_name, enrollment_status, attendance_rate (decimal?), progress_percent (decimal?), completion_status, notes
    8. `HealthWellbeingRecord.cs` -- PK: health_record_id, FK: resident_id, columns: record_date, general_health_score (decimal?), nutrition_score (decimal?), sleep_quality_score (decimal?), energy_level_score (decimal?), height_cm (decimal?), weight_kg (decimal?), bmi (decimal?), medical_checkup_done (bool), dental_checkup_done (bool), psychological_checkup_done (bool), notes
    9. `ProcessRecording.cs` -- PK: recording_id, FK: resident_id, columns: session_date, social_worker, session_type, session_duration_minutes (int?), emotional_state_observed, emotional_state_end, session_narrative, interventions_applied, follow_up_actions, progress_noted, concerns_flagged, referral_made, notes_restricted
    10. `HomeVisitation.cs` -- PK: visitation_id, FK: resident_id, columns: visit_date, social_worker, visit_type, location_visited, family_members_present, purpose, observations, family_cooperation_level, safety_concerns_noted, follow_up_needed (bool), follow_up_notes, visit_outcome
    11. `IncidentReport.cs` -- PK: incident_id, FKs: resident_id, safehouse_id, columns: incident_date, incident_type, severity, description, response_taken, resolved (bool), resolution_date, reported_by, follow_up_required (bool)
    12. `InterventionPlan.cs` -- PK: plan_id, FK: resident_id, columns: plan_category, plan_description, services_provided, target_value, target_date, status, case_conference_date, created_at, updated_at
    13. `Partner.cs` -- PK: partner_id, columns: partner_name, partner_type, role_type, contact_name, email, phone, region, status, start_date, end_date, notes
    14. `PartnerAssignment.cs` -- PK: assignment_id, FKs: partner_id, safehouse_id, columns: program_area, assignment_start, assignment_end, responsibility_notes, is_primary (bool), status
    15. `SocialMediaPost.cs` -- PK: post_id, many numeric columns (impressions, reach, likes, comments, shares, saves, click_throughs, video_views as int?), engagement_rate as decimal?, forward as int?, etc.
    16. `SafehouseMonthlyMetric.cs` -- PK: metric_id, FK: safehouse_id, columns: month_start, month_end, active_residents (int), avg_education_progress (decimal?), avg_health_score (decimal?), process_recording_count (int), home_visitation_count (int), incident_count (int), notes
    17. `PublicImpactSnapshot.cs` -- PK: snapshot_id, columns: snapshot_date, headline, summary_text, metric_payload_json (string), is_published (bool), published_at

    **Step 3: Create AppDbContext.cs**

    - 17 `DbSet<T>` properties, one per entity
    - In `OnModelCreating`:
      - Configure all foreign key relationships with `.HasOne().WithMany().HasForeignKey()`
      - Add indexes per RESEARCH.md DATA-03:
        ```csharp
        // Resident indexes
        modelBuilder.Entity<Resident>().HasIndex(r => r.SafehouseId);
        modelBuilder.Entity<Resident>().HasIndex(r => r.CaseStatus);
        modelBuilder.Entity<Resident>().HasIndex(r => r.CurrentRiskLevel);
        // Donation indexes
        modelBuilder.Entity<Donation>().HasIndex(d => d.SupporterId);
        modelBuilder.Entity<Donation>().HasIndex(d => d.DonationDate);
        // ProcessRecording indexes
        modelBuilder.Entity<ProcessRecording>().HasIndex(p => p.ResidentId);
        modelBuilder.Entity<ProcessRecording>().HasIndex(p => p.SessionDate);
        // HomeVisitation indexes
        modelBuilder.Entity<HomeVisitation>().HasIndex(h => h.ResidentId);
        // DonationAllocation indexes
        modelBuilder.Entity<DonationAllocation>().HasIndex(da => da.DonationId);
        modelBuilder.Entity<DonationAllocation>().HasIndex(da => da.SafehouseId);
        ```
      - Configure decimal precision for currency columns: `.HasPrecision(18, 2)` for amount, estimated_value, amount_allocated, estimated_unit_value, boost_budget_php, estimated_donation_value_php
      - Use `decimal(10, 4)` for engagement_rate, attendance_rate, progress_percent

    **Step 4: Create SeedData.cs**

    - Static class with `public static async Task SeedAsync(AppDbContext context)`
    - Guard clause: `if (await context.Safehouses.AnyAsync()) return;` (skip if already seeded)
    - Seed in exact dependency order from RESEARCH.md:
      1. safehouses (9 rows)
      2. residents (60 rows)
      3. supporters (60 rows)
      4. partners (30 rows)
      5. donations (420 rows)
      6. donation_allocations (521 rows)
      7. in_kind_donation_items (129 rows)
      8. education_records (534 rows)
      9. health_wellbeing_records (534 rows)
      10. process_recordings (2819 rows)
      11. home_visitations (1337 rows)
      12. incident_reports (100 rows)
      13. intervention_plans (180 rows)
      14. partner_assignments (48 rows)
      15. social_media_posts (812 rows)
      16. safehouse_monthly_metrics (450 rows)
      17. public_impact_snapshots (50 rows)
    - CSV path: `Path.Combine(AppContext.BaseDirectory, "..", "..", "..", "..", "..", "data", "lighthouse_csv_v7")` for development. Also check `Path.Combine(AppContext.BaseDirectory, "data", "lighthouse_csv_v7")` for production.
    - Use CsvHelper with `CultureInfo.InvariantCulture`
    - Create CsvHelper ClassMaps for entities where column names need snake_case-to-PascalCase mapping. Use `AutoMap` with `PrepareHeaderForMatch` to handle snake_case: `csv.Context.TypeConverterOptionsCache` and `csv.Configuration.PrepareHeaderForMatch = args => args.Header.Replace("_", "").ToLower();` -- but verify this works with actual CSV headers. A simpler approach: use `[Name("snake_case_name")]` attributes on entity properties OR use ClassMaps.
    - Handle special cases:
      - `partner_assignments.safehouse_id` has "8.0" format -- use a custom TypeConverter or ClassMap that parses as double then casts to int
      - `public_impact_snapshots.metric_payload_json` -- read as plain string
      - Nullable date fields -- CsvHelper handles empty strings as null for DateTime? automatically
      - Boolean fields with True/False text -- CsvHelper handles this automatically
    - After adding each batch, call `await context.SaveChangesAsync()` to get IDs for FK dependencies
    - Log each table seeded with row count: `Console.WriteLine($"Seeded {count} {tableName}");`

    **Step 5: Create minimal Program.cs**

    This is a MINIMAL Program.cs for Plan 01 only (auth will be added in Plan 02):
    ```csharp
    var builder = WebApplication.CreateBuilder(args);

    builder.Services.AddControllers();
    builder.Services.AddEndpointsApiExplorer();
    builder.Services.AddSwaggerGen();

    // PostgreSQL
    var connString = builder.Configuration.GetConnectionString("DefaultConnection");
    builder.Services.AddDbContext<AppDbContext>(options => options.UseNpgsql(connString));

    // CORS for frontend dev
    builder.Services.AddCors(options =>
    {
        options.AddPolicy("FrontendClient", policy =>
        {
            policy.WithOrigins("http://localhost:3000")
                .AllowCredentials()
                .AllowAnyMethod()
                .AllowAnyHeader();
        });
    });

    var app = builder.Build();

    AppContext.SetSwitch("Npgsql.EnableLegacyTimestampBehavior", true);

    // Apply migrations + seed
    using (var scope = app.Services.CreateScope())
    {
        var appDb = scope.ServiceProvider.GetRequiredService<AppDbContext>();
        await appDb.Database.MigrateAsync();
        await SeedData.SeedAsync(appDb);
    }

    if (app.Environment.IsDevelopment())
    {
        app.UseSwagger();
        app.UseSwaggerUI();
    }

    app.UseCors("FrontendClient");
    app.UseHttpsRedirection();
    app.MapControllers();
    app.Run();
    ```

    **Step 6: Configure appsettings**

    `appsettings.json`:
    ```json
    {
      "Logging": { "LogLevel": { "Default": "Information" } },
      "AllowedHosts": "*"
    }
    ```

    `appsettings.Development.json`:
    ```json
    {
      "Logging": { "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" } },
      "ConnectionStrings": {
        "DefaultConnection": "Host=localhost;Database=harbor_of_hope;Username=postgres;Password=postgres"
      },
      "FrontendUrl": "http://localhost:3000"
    }
    ```

    **Step 7: Generate initial migration and verify**
    ```bash
    cd backend/HarborOfHope.API
    dotnet ef migrations add InitApp --context AppDbContext --output-dir Migrations/Harbor
    dotnet build
    ```
  </action>
  <verify>
    <automated>cd "/Users/waylansmac/INTEX 2026/backend/HarborOfHope.API" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds with 0 errors
    - File `backend/HarborOfHope.API/Data/AppDbContext.cs` exists and contains `DbSet<Safehouse>`, `DbSet<Resident>`, `DbSet<Supporter>`, `DbSet<Donation>` (4 of 17 DbSets)
    - File `backend/HarborOfHope.API/Data/AppDbContext.cs` contains `.HasIndex(r => r.SafehouseId)` and `.HasIndex(d => d.DonationDate)`
    - File `backend/HarborOfHope.API/Data/Entities/Resident.cs` exists and contains properties `ResidentId`, `CaseControlNo`, `SafehouseId`, `CaseStatus`, `CurrentRiskLevel`
    - File `backend/HarborOfHope.API/Data/SeedData.cs` exists and contains `CsvReader` and `SeedAsync`
    - File `backend/HarborOfHope.API/Data/SeedData.cs` seeds in dependency order: safehouses before residents, supporters before donations
    - All 17 entity files exist in `backend/HarborOfHope.API/Data/Entities/`
    - File `backend/HarborOfHope.API/Program.cs` contains `UseNpgsql` and `SeedData.SeedAsync`
    - Migration directory `backend/HarborOfHope.API/Migrations/Harbor/` exists with at least one migration file
    - `appsettings.Development.json` contains `"DefaultConnection"` with `Host=localhost;Database=harbor_of_hope`
    - `HarborOfHope.API.csproj` references `Npgsql.EntityFrameworkCore.PostgreSQL`, `CsvHelper`, `Swashbuckle.AspNetCore`
  </acceptance_criteria>
  <done>Backend project compiles, 17 entity classes match CSV columns, AppDbContext has all DbSets with proper indexes, SeedData reads from all 17 CSV files in dependency order, migration generated successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold frontend project with MUI theme, Vite proxy, and basic app shell</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/theme.ts
  </files>
  <read_first>
    /Users/waylansmac/INTEX 2026/.planning/research/STACK.md
    /Users/waylansmac/INTEX 2026/.planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 4: Vite Proxy, and MUI theme section)
  </read_first>
  <action>
    **Step 1: Scaffold Vite React TypeScript project**
    ```bash
    cd "/Users/waylansmac/INTEX 2026"
    npm create vite@latest frontend -- --template react-ts
    cd frontend
    ```

    **Step 2: Install all Phase 1 frontend dependencies**
    ```bash
    # MUI v6 + Emotion
    npm install @mui/material@6 @emotion/react @emotion/styled

    # Routing + HTTP
    npm install react-router-dom axios

    # QR code for MFA (needed in Plan 03)
    npm install qrcode
    npm install -D @types/qrcode
    ```

    **Step 3: Create `vite.config.ts`**
    ```typescript
    import { defineConfig } from 'vite';
    import react from '@vitejs/plugin-react';

    export default defineConfig({
      plugins: [react()],
      server: {
        port: 3000,
        proxy: {
          '/api': {
            target: 'https://localhost:5001',
            changeOrigin: true,
            secure: false,
          },
          '/signin-google': {
            target: 'https://localhost:5001',
            changeOrigin: true,
            secure: false,
          },
        },
      },
      build: {
        assetsInlineLimit: 0,
      },
    });
    ```

    **Step 4: Create `src/theme.ts`** with the warm nonprofit MUI theme
    ```typescript
    import { createTheme } from '@mui/material/styles';

    const theme = createTheme({
      palette: {
        primary: { main: '#E8735A' },
        secondary: { main: '#5B8C7A' },
        background: {
          default: '#FFF8F0',
          paper: '#FFFFFF',
        },
        text: {
          primary: '#2D2D2D',
          secondary: '#6B6B6B',
        },
      },
      typography: {
        fontFamily: '"Nunito", sans-serif',
        h1: { fontWeight: 800 },
        h2: { fontWeight: 700 },
        h3: { fontWeight: 700 },
        h4: { fontWeight: 700 },
        h5: { fontWeight: 600 },
        h6: { fontWeight: 600 },
        button: { fontWeight: 700, textTransform: 'none' as const },
      },
      shape: {
        borderRadius: 12,
      },
      components: {
        MuiCard: {
          styleOverrides: {
            root: { boxShadow: '0 2px 12px rgba(0,0,0,0.08)' },
          },
        },
        MuiButton: {
          styleOverrides: {
            root: { borderRadius: 24 },
          },
        },
      },
    });

    export default theme;
    ```

    **Step 5: Update `index.html`** to include Nunito font
    Add to `<head>`:
    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
    ```
    Set `<title>Harbor of Hope</title>`

    **Step 6: Update `src/main.tsx`**
    ```typescript
    import React from 'react';
    import ReactDOM from 'react-dom/client';
    import { ThemeProvider } from '@mui/material/styles';
    import CssBaseline from '@mui/material/CssBaseline';
    import { BrowserRouter } from 'react-router-dom';
    import App from './App';
    import theme from './theme';

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <ThemeProvider theme={theme}>
          <CssBaseline />
          <BrowserRouter>
            <App />
          </BrowserRouter>
        </ThemeProvider>
      </React.StrictMode>,
    );
    ```

    **Step 7: Update `src/App.tsx`** with placeholder routing
    ```typescript
    import { Routes, Route } from 'react-router-dom';
    import { Typography, Container } from '@mui/material';

    function App() {
      return (
        <Container maxWidth="lg" sx={{ py: 4 }}>
          <Routes>
            <Route path="/" element={<Typography variant="h2">Harbor of Hope</Typography>} />
          </Routes>
        </Container>
      );
    }

    export default App;
    ```

    **Step 8: Verify frontend starts**
    ```bash
    cd frontend && npm run dev -- --host 2>&1 &
    sleep 3
    curl -s http://localhost:3000 | head -5
    kill %1
    ```
  </action>
  <verify>
    <automated>cd "/Users/waylansmac/INTEX 2026/frontend" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with 0 errors
    - File `frontend/package.json` contains `"@mui/material"`, `"react-router-dom"`, `"axios"`, `"@emotion/react"`
    - File `frontend/vite.config.ts` contains `proxy` and `target: 'https://localhost:5001'` and `'/api'`
    - File `frontend/vite.config.ts` contains `'/signin-google'` proxy entry
    - File `frontend/src/theme.ts` contains `'#E8735A'` and `'#FFF8F0'` and `'"Nunito"'`
    - File `frontend/src/main.tsx` contains `ThemeProvider` and `BrowserRouter` and `CssBaseline`
    - File `frontend/index.html` contains `fonts.googleapis.com` and `Nunito` and `Harbor of Hope`
    - File `frontend/src/App.tsx` contains `Routes` and `Route`
  </acceptance_criteria>
  <done>Frontend dev server starts on port 3000, TypeScript compiles without errors, MUI theme renders coral/cream palette, Vite proxies /api to backend.</done>
</task>

</tasks>

<verification>
- `cd backend/HarborOfHope.API && dotnet build` succeeds
- `cd frontend && npx tsc --noEmit` succeeds
- 17 entity files exist in `backend/HarborOfHope.API/Data/Entities/`
- AppDbContext.cs contains 17 DbSet properties
- SeedData.cs references all 17 CSV files
- Migration directory has files
- Frontend package.json has MUI, React Router, axios
</verification>

<success_criteria>
Backend compiles with all 17 entities, AppDbContext, indexes, and CSV seeder. Frontend compiles with MUI theme, Vite proxy, and React Router shell. Both projects are ready for Plan 02 to add authentication.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
