---
phase: 02-admin-crud-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/HarborOfHope.API/DTOs/PagedResult.cs
  - backend/HarborOfHope.API/DTOs/ResidentListDto.cs
  - backend/HarborOfHope.API/DTOs/ResidentDetailDto.cs
  - backend/HarborOfHope.API/DTOs/ResidentCreateDto.cs
  - backend/HarborOfHope.API/DTOs/SupporterDto.cs
  - backend/HarborOfHope.API/DTOs/SupporterCreateDto.cs
  - backend/HarborOfHope.API/DTOs/DonationDto.cs
  - backend/HarborOfHope.API/DTOs/DonationCreateDto.cs
  - backend/HarborOfHope.API/DTOs/ProcessRecordingDto.cs
  - backend/HarborOfHope.API/DTOs/ProcessRecordingCreateDto.cs
  - backend/HarborOfHope.API/DTOs/HomeVisitationDto.cs
  - backend/HarborOfHope.API/DTOs/HomeVisitationCreateDto.cs
  - backend/HarborOfHope.API/DTOs/DashboardStatsDto.cs
  - backend/HarborOfHope.API/Infrastructure/InputSanitizer.cs
  - backend/HarborOfHope.API/Controllers/ResidentsController.cs
  - backend/HarborOfHope.API/Controllers/SupportersController.cs
  - backend/HarborOfHope.API/Controllers/DonationsController.cs
  - backend/HarborOfHope.API/Controllers/ProcessRecordingsController.cs
  - backend/HarborOfHope.API/Controllers/HomeVisitationsController.cs
  - backend/HarborOfHope.API/Controllers/DashboardController.cs
autonomous: true
requirements: [SEC-04, SEC-05]

must_haves:
  truths:
    - "GET /api/residents returns paginated, sortable, filterable resident list with search"
    - "GET/POST/PUT/DELETE /api/residents provides full CRUD"
    - "GET/POST/PUT/DELETE /api/supporters provides full CRUD with pagination"
    - "GET/POST/PUT/DELETE /api/donations provides full CRUD with pagination"
    - "GET/POST/PUT/DELETE /api/processrecordings provides full CRUD with resident filter"
    - "GET/POST/PUT/DELETE /api/homevisitations provides full CRUD with resident filter"
    - "GET /api/dashboard returns aggregated stats (4 metrics + recent tables)"
    - "All string inputs are HTML-encoded server-side before saving"
    - "All controllers require AdminOnly policy"
  artifacts:
    - path: "backend/HarborOfHope.API/DTOs/PagedResult.cs"
      provides: "Generic pagination wrapper"
      contains: "class PagedResult"
    - path: "backend/HarborOfHope.API/Controllers/ResidentsController.cs"
      provides: "Resident CRUD with search/filter/sort/pagination"
      exports: ["GetAll", "GetById", "Create", "Update", "Delete"]
    - path: "backend/HarborOfHope.API/Controllers/DashboardController.cs"
      provides: "Dashboard aggregation endpoint"
      exports: ["GetDashboardStats"]
    - path: "backend/HarborOfHope.API/Infrastructure/InputSanitizer.cs"
      provides: "Server-side HTML encoding for all string inputs"
      contains: "HtmlEncoder"
  key_links:
    - from: "Controllers/ResidentsController.cs"
      to: "Data/AppDbContext.cs"
      via: "EF Core queries with Include, Where, OrderBy"
      pattern: "db\\.Residents"
    - from: "Controllers/DashboardController.cs"
      to: "Data/AppDbContext.cs"
      via: "Aggregation queries (Count, Sum)"
      pattern: "db\\.Residents\\.CountAsync"
---

<objective>
Build all 6 backend API controllers with DTOs, pagination, and server-side input sanitization for the admin CRUD dashboard phase.

Purpose: The frontend CRUD pages and dashboard all depend on these endpoints. Building backend first allows Swagger testing before any frontend work.
Output: 6 fully functional API controllers (Residents, Supporters, Donations, ProcessRecordings, HomeVisitations, Dashboard) with DTOs and input sanitization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-crud-dashboard/02-CONTEXT.md
@.planning/phases/02-admin-crud-dashboard/02-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From backend/HarborOfHope.API/Data/AppDbContext.cs:
- 17 DbSets: Residents, Supporters, Donations, ProcessRecordings, HomeVisitations, Safehouses, etc.
- All relationships configured in OnModelCreating
- Primary constructor: AppDbContext(DbContextOptions<AppDbContext> options)

From backend/HarborOfHope.API/Data/AuthPolicies.cs:
```csharp
public static class AuthPolicies
{
    public const string AdminOnly = "AdminOnly";
}
```

From backend/HarborOfHope.API/Data/Entities/Resident.cs (47 columns):
- Key: ResidentId (int)
- FK: SafehouseId -> Safehouse
- Key display fields: CaseControlNo, InternalCode, CaseStatus, CaseCategory, CurrentRiskLevel, SafehouseId
- 10 boolean sub-categories: SubCatOrphaned, SubCatTrafficked, SubCatChildLabor, SubCatPhysicalAbuse, SubCatSexualAbuse, SubCatOsaec, SubCatCicl, SubCatAtRisk, SubCatStreetChild, SubCatChildWithHiv
- Sensitive field to EXCLUDE from list DTOs: NotesRestricted
- Navigation: Safehouse, EducationRecords, HealthWellbeingRecords, ProcessRecordings, HomeVisitations, IncidentReports, InterventionPlans

From backend/HarborOfHope.API/Data/Entities/Supporter.cs:
- Key: SupporterId (int)
- Fields: SupporterType, DisplayName, OrganizationName, FirstName, LastName, RelationshipType, Region, Country, Email, Phone, Status, CreatedAt, FirstDonationDate, AcquisitionChannel
- Navigation: Donations (collection)

From backend/HarborOfHope.API/Data/Entities/Donation.cs:
- Key: DonationId (int), FK: SupporterId
- Fields: DonationType, DonationDate, IsRecurring, CampaignName, ChannelSource, CurrencyCode, Amount (decimal), EstimatedValue (decimal?), ImpactUnit, Notes, ReferralPostId
- Navigation: Supporter, DonationAllocations, InKindDonationItems

From backend/HarborOfHope.API/Data/Entities/ProcessRecording.cs:
- Key: RecordingId (int), FK: ResidentId
- Fields: SessionDate, SocialWorker, SessionType, SessionDurationMinutes, EmotionalStateObserved, EmotionalStateEnd, SessionNarrative, InterventionsApplied, FollowUpActions, ProgressNoted, ConcernsFlagged, ReferralMade, NotesRestricted
- Navigation: Resident

From backend/HarborOfHope.API/Data/Entities/HomeVisitation.cs:
- Key: VisitationId (int), FK: ResidentId
- Fields: VisitDate, SocialWorker, VisitType, LocationVisited, FamilyMembersPresent, Purpose, Observations, FamilyCooperationLevel, SafetyConcernsNoted, FollowUpNeeded, FollowUpNotes, VisitOutcome
- Navigation: Resident

From backend/HarborOfHope.API/Data/Entities/Safehouse.cs:
- Key: SafehouseId (int)
- Fields: SafehouseCode, Name, Region, City, Province, Country, OpenDate, Status, CapacityGirls, CapacityStaff, CurrentOccupancy, Notes

From backend/HarborOfHope.API/Controllers/AuthController.cs:
- Uses primary constructor injection pattern: `AuthController(UserManager<ApplicationUser> userManager, ...)`
- Uses `[Authorize]` and `[AllowAnonymous]` attributes
- Returns ActionResult<T> for typed responses
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all DTOs and InputSanitizer</name>
  <files>
    backend/HarborOfHope.API/DTOs/PagedResult.cs,
    backend/HarborOfHope.API/DTOs/ResidentListDto.cs,
    backend/HarborOfHope.API/DTOs/ResidentDetailDto.cs,
    backend/HarborOfHope.API/DTOs/ResidentCreateDto.cs,
    backend/HarborOfHope.API/DTOs/SupporterDto.cs,
    backend/HarborOfHope.API/DTOs/SupporterCreateDto.cs,
    backend/HarborOfHope.API/DTOs/DonationDto.cs,
    backend/HarborOfHope.API/DTOs/DonationCreateDto.cs,
    backend/HarborOfHope.API/DTOs/ProcessRecordingDto.cs,
    backend/HarborOfHope.API/DTOs/ProcessRecordingCreateDto.cs,
    backend/HarborOfHope.API/DTOs/HomeVisitationDto.cs,
    backend/HarborOfHope.API/DTOs/HomeVisitationCreateDto.cs,
    backend/HarborOfHope.API/DTOs/DashboardStatsDto.cs,
    backend/HarborOfHope.API/Infrastructure/InputSanitizer.cs
  </files>
  <read_first>
    backend/HarborOfHope.API/Data/Entities/Resident.cs,
    backend/HarborOfHope.API/Data/Entities/Supporter.cs,
    backend/HarborOfHope.API/Data/Entities/Donation.cs,
    backend/HarborOfHope.API/Data/Entities/ProcessRecording.cs,
    backend/HarborOfHope.API/Data/Entities/HomeVisitation.cs,
    backend/HarborOfHope.API/Data/Entities/Safehouse.cs,
    backend/HarborOfHope.API/Data/AppDbContext.cs
  </read_first>
  <action>
    Create `backend/HarborOfHope.API/DTOs/` directory and all DTO classes.

    **PagedResult.cs** -- Generic pagination wrapper (per D-01 manual pagination):
    ```csharp
    namespace HarborOfHope.API.DTOs;
    public class PagedResult<T>
    {
        public List<T> Items { get; set; } = [];
        public int TotalCount { get; set; }
        public int Page { get; set; }
        public int PageSize { get; set; }
        public int TotalPages => (int)Math.Ceiling((double)TotalCount / PageSize);
    }
    ```

    **ResidentListDto.cs** -- Slim DTO for table rows (5-6 key columns per D-02). Include: ResidentId, CaseControlNo, InternalCode, SafehouseId, SafehouseName (string from Include), CaseStatus, CaseCategory, CurrentRiskLevel, DateOfAdmission, PresentAge. OMIT NotesRestricted and all boolean sub-categories.

    **ResidentDetailDto.cs** -- Full DTO for edit form and expandable row. Include ALL 47 fields from Resident entity EXCEPT NotesRestricted. Include SafehouseName. Include all 10 sub-category booleans, all dates, all string fields.

    **ResidentCreateDto.cs** -- Input DTO for POST/PUT. All fields from Resident entity that are user-editable (exclude ResidentId, CreatedAt, navigation props). All string fields nullable. SafehouseId required (int).

    **SupporterDto.cs** -- Full supporter for table and detail. Include all Supporter fields. Include DonationCount (int) computed from navigation.

    **SupporterCreateDto.cs** -- Input for POST/PUT. All editable Supporter fields (exclude SupporterId, CreatedAt).

    **DonationDto.cs** -- Donation with supporter name. Include all Donation fields + SupporterDisplayName (string).

    **DonationCreateDto.cs** -- Input for POST/PUT. All editable Donation fields (exclude DonationId). SupporterId required.

    **ProcessRecordingDto.cs** -- Session with resident info. Include all ProcessRecording fields EXCEPT NotesRestricted + ResidentCode (string from navigation).

    **ProcessRecordingCreateDto.cs** -- Input for POST/PUT. All editable fields (exclude RecordingId). ResidentId required.

    **HomeVisitationDto.cs** -- Visit with resident info. Include all HomeVisitation fields + ResidentCode (string from navigation).

    **HomeVisitationCreateDto.cs** -- Input for POST/PUT. All editable fields (exclude VisitationId). ResidentId required.

    **DashboardStatsDto.cs** -- Dashboard aggregation response:
    ```csharp
    public class DashboardStatsDto
    {
        public int TotalResidents { get; set; }
        public int ActiveCases { get; set; }
        public decimal TotalDonations { get; set; }
        public double ReintegrationRate { get; set; }
        public List<RecentDonationDto> RecentDonations { get; set; } = [];
        public List<AttentionResidentDto> ResidentsNeedingAttention { get; set; } = [];
    }
    public class RecentDonationDto
    {
        public int DonationId { get; set; }
        public string? SupporterName { get; set; }
        public decimal Amount { get; set; }
        public string? DonationType { get; set; }
        public DateTime? DonationDate { get; set; }
    }
    public class AttentionResidentDto
    {
        public int ResidentId { get; set; }
        public string? CaseControlNo { get; set; }
        public string? SafehouseName { get; set; }
        public string? CurrentRiskLevel { get; set; }
        public string? CaseStatus { get; set; }
    }
    ```

    **InputSanitizer.cs** -- Server-side HTML encoding helper for SEC-04:
    ```csharp
    using System.Text.Encodings.Web;
    namespace HarborOfHope.API.Infrastructure;
    public static class InputSanitizer
    {
        public static string? Sanitize(string? input)
            => input is null ? null : HtmlEncoder.Default.Encode(input);
    }
    ```

    All DTOs use namespace `HarborOfHope.API.DTOs`. Use file-scoped namespaces. No data annotations on DTOs (validation happens via zod on frontend and model binding on backend).
  </action>
  <verify>
    <automated>cd "/Users/waylansmac/INTEX 2026/backend/HarborOfHope.API" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/HarborOfHope.API/DTOs/` shows all 13 DTO files
    - `grep -r "class PagedResult" backend/HarborOfHope.API/DTOs/` returns match
    - `grep -r "NotesRestricted" backend/HarborOfHope.API/DTOs/ResidentListDto.cs` returns NO match (sensitive field excluded from list)
    - `grep -r "HtmlEncoder" backend/HarborOfHope.API/Infrastructure/InputSanitizer.cs` returns match
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>All 13 DTO files and InputSanitizer created, build passes, NotesRestricted excluded from list DTO</done>
</task>

<task type="auto">
  <name>Task 2: Create all 6 API controllers</name>
  <files>
    backend/HarborOfHope.API/Controllers/ResidentsController.cs,
    backend/HarborOfHope.API/Controllers/SupportersController.cs,
    backend/HarborOfHope.API/Controllers/DonationsController.cs,
    backend/HarborOfHope.API/Controllers/ProcessRecordingsController.cs,
    backend/HarborOfHope.API/Controllers/HomeVisitationsController.cs,
    backend/HarborOfHope.API/Controllers/DashboardController.cs
  </files>
  <read_first>
    backend/HarborOfHope.API/Controllers/AuthController.cs,
    backend/HarborOfHope.API/Data/AppDbContext.cs,
    backend/HarborOfHope.API/Data/AuthPolicies.cs,
    backend/HarborOfHope.API/DTOs/PagedResult.cs,
    backend/HarborOfHope.API/DTOs/ResidentListDto.cs,
    backend/HarborOfHope.API/DTOs/ResidentCreateDto.cs,
    backend/HarborOfHope.API/Infrastructure/InputSanitizer.cs
  </read_first>
  <action>
    Create all 6 controllers in `backend/HarborOfHope.API/Controllers/`. All controllers use:
    - `[ApiController]`, `[Route("api/[controller]")]`, `[Authorize(Policy = AuthPolicies.AdminOnly)]`
    - Primary constructor injection: `ControllerName(AppDbContext db) : ControllerBase`
    - File-scoped namespace: `namespace HarborOfHope.API.Controllers;`
    - Using statements: `using HarborOfHope.API.Data; using HarborOfHope.API.Data.Entities; using HarborOfHope.API.DTOs; using HarborOfHope.API.Infrastructure; using Microsoft.AspNetCore.Authorization; using Microsoft.AspNetCore.Mvc; using Microsoft.EntityFrameworkCore;`

    **ResidentsController.cs** -- CRUD + search/filter/sort/pagination:
    - `[HttpGet]` GetAll: Query params -- page (default 1), pageSize (default 10), sortBy (default "residentId"), sortDir (default "asc"), search (string?), safehouseId (int?), status (string?), riskLevel (string?), category (string?). Include Safehouse for name. Search on CaseControlNo and InternalCode using Contains. Sort switch on residentId, caseStatus, safehouseId, currentRiskLevel, dateOfAdmission. Return PagedResult<ResidentListDto>.
    - `[HttpGet("{id}")]` GetById: Include Safehouse. Return ResidentDetailDto. 404 if not found.
    - `[HttpPost]` Create: Accept ResidentCreateDto. Sanitize ALL string fields using InputSanitizer.Sanitize(). Set CreatedAt = DateTime.UtcNow. Map to Resident entity, save, return 201 with ResidentDetailDto.
    - `[HttpPut("{id}")]` Update: Find by id (404 if missing). Sanitize all string inputs. Map dto fields onto entity. Save. Return NoContent.
    - `[HttpDelete("{id}")]` Delete: Find by id (404 if missing). Remove. Save. Return NoContent.

    **SupportersController.cs** -- CRUD + pagination:
    - `[HttpGet]` GetAll: page, pageSize, sortBy (default "supporterId"), sortDir, search (on DisplayName, Email). Include Donations for count. Return PagedResult<SupporterDto> with DonationCount = s.Donations.Count.
    - `[HttpGet("{id}")]` GetById with Donations included. Return SupporterDto.
    - `[HttpPost]` Create: Sanitize strings, set CreatedAt = DateTime.UtcNow. Return 201.
    - `[HttpPut("{id}")]` Update: Sanitize, map, save, NoContent.
    - `[HttpDelete("{id}")]` Delete: 404 if missing, remove, NoContent.

    **DonationsController.cs** -- CRUD + pagination:
    - `[HttpGet]` GetAll: page, pageSize, sortBy (default "donationDate"), sortDir (default "desc"), supporterId (int? filter). Include Supporter for name. Return PagedResult<DonationDto>.
    - `[HttpGet("{id}")]` GetById. Return DonationDto.
    - `[HttpPost]` Create: Validate SupporterId exists (400 if not). Sanitize strings. Return 201.
    - `[HttpPut("{id}")]` Update. `[HttpDelete("{id}")]` Delete.

    **ProcessRecordingsController.cs** -- CRUD + resident filter:
    - `[HttpGet]` GetAll: page, pageSize, sortBy (default "sessionDate"), sortDir (default "desc"), residentId (int? filter). Include Resident for CaseControlNo. Return PagedResult<ProcessRecordingDto>.
    - `[HttpGet("{id}")]` GetById. `[HttpPost]` Create (validate ResidentId exists). `[HttpPut("{id}")]` Update. `[HttpDelete("{id}")]` Delete.
    - Sanitize all string fields. EXCLUDE NotesRestricted from DTO response.

    **HomeVisitationsController.cs** -- CRUD + resident filter:
    - `[HttpGet]` GetAll: page, pageSize, sortBy (default "visitDate"), sortDir (default "desc"), residentId (int? filter). Include Resident for CaseControlNo. Return PagedResult<HomeVisitationDto>.
    - `[HttpGet("{id}")]` GetById. `[HttpPost]` Create (validate ResidentId exists). `[HttpPut("{id}")]` Update. `[HttpDelete("{id}")]` Delete.
    - Sanitize all string fields.

    **DashboardController.cs** -- Read-only aggregation (per D-04, D-05, D-06, D-07):
    - `[HttpGet]` GetDashboardStats: Single endpoint returning DashboardStatsDto. Compute:
      - TotalResidents = Residents.CountAsync()
      - ActiveCases = Residents.CountAsync(r => r.CaseStatus == "Active")
      - TotalDonations = Donations.SumAsync(d => d.Amount) -- wrap in try/catch for empty table returning 0
      - ReintegrationRate = (completed / total_with_type) * 100, rounded to 1 decimal. Where completed = ReintegrationStatus == "Completed", total_with_type = ReintegrationType != null. If total_with_type == 0, rate = 0.
      - RecentDonations = top 5 by DonationDate desc, Include Supporter, map to RecentDonationDto
      - ResidentsNeedingAttention = Residents where CurrentRiskLevel is "Critical" or "High", ordered Critical first then High, take 5, Include Safehouse, map to AttentionResidentDto

    All string inputs on Create/Update actions MUST be sanitized via InputSanitizer.Sanitize() before saving to database (SEC-04 compliance).
  </action>
  <verify>
    <automated>cd "/Users/waylansmac/INTEX 2026/backend/HarborOfHope.API" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/HarborOfHope.API/Controllers/` shows ResidentsController.cs, SupportersController.cs, DonationsController.cs, ProcessRecordingsController.cs, HomeVisitationsController.cs, DashboardController.cs
    - `grep -c "Authorize.*AdminOnly" backend/HarborOfHope.API/Controllers/ResidentsController.cs` returns 1
    - `grep -c "InputSanitizer.Sanitize" backend/HarborOfHope.API/Controllers/ResidentsController.cs` returns at least 1
    - `grep -c "HttpDelete" backend/HarborOfHope.API/Controllers/ResidentsController.cs` returns 1
    - `grep "PagedResult" backend/HarborOfHope.API/Controllers/ResidentsController.cs` returns match
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>All 6 controllers created with full CRUD, pagination, search/filter, input sanitization, AdminOnly authorization. Build passes.</done>
</task>

</tasks>

<verification>
- `dotnet build` in backend directory succeeds with 0 errors
- All 6 controllers exist in Controllers/ directory
- All 13 DTO files exist in DTOs/ directory
- InputSanitizer.cs exists in Infrastructure/ directory
- All controllers use [Authorize(Policy = AuthPolicies.AdminOnly)]
- All Create/Update actions call InputSanitizer.Sanitize() on string fields
- ResidentsController supports search, filter by safehouse/status/riskLevel/category, sort, pagination
- DashboardController returns 4 metrics + 2 tables
- No DTO exposes NotesRestricted field
</verification>

<success_criteria>
- Backend compiles with 0 errors after adding all controllers and DTOs
- 20 new files created (13 DTOs + 6 controllers + 1 sanitizer)
- All endpoints follow the established controller pattern (primary constructor, ApiController attribute)
- Input sanitization applied to all string fields on Create/Update actions
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-crud-dashboard/02-01-SUMMARY.md`
</output>
