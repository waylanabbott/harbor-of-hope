---
phase: 03-public-pages-donor-portal
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/pages/public/LandingPage.tsx
  - frontend/src/pages/public/PublicImpactPage.tsx
  - frontend/src/pages/public/PrivacyPolicyPage.tsx
  - frontend/src/pages/donor/DonorDashboard.tsx
  - frontend/src/pages/donor/DonorHistoryPage.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - PORTAL-01
  - PORTAL-02

must_haves:
  truths:
    - "Visitor can view landing page with hero image, mission cards, impact statistics, and Donate Now CTA"
    - "Visitor can view public impact dashboard showing anonymized charts (health scores, donations over time, resident counts)"
    - "Visitor can access privacy policy page from the footer link on any page"
    - "Visitor sees cookie consent banner on first visit with Accept and Decline buttons"
    - "Visitor can toggle dark mode from the AppBar and preference persists via cookie after consent"
    - "Donor can log in and see their own donation history table"
    - "Donor can see their impact summary with total donated, donation count, and allocation breakdown"
  artifacts:
    - path: "frontend/src/pages/public/LandingPage.tsx"
      provides: "Hero section, mission cards, live impact stats from PublicController, Donate Now CTA"
      min_lines: 80
    - path: "frontend/src/pages/public/PublicImpactPage.tsx"
      provides: "Recharts line/bar charts with anonymized monthly impact data"
      min_lines: 60
    - path: "frontend/src/pages/public/PrivacyPolicyPage.tsx"
      provides: "Harbor of Hope specific privacy policy content"
      min_lines: 40
    - path: "frontend/src/pages/donor/DonorDashboard.tsx"
      provides: "Donor impact summary with MetricCards and allocation table"
      min_lines: 60
    - path: "frontend/src/pages/donor/DonorHistoryPage.tsx"
      provides: "Donor donation history in a paginated table"
      min_lines: 50
    - path: "frontend/src/App.tsx"
      provides: "All routes wired: /, /impact, /privacy, /donor/dashboard, /donor/donations"
      contains: "LandingPage"
  key_links:
    - from: "frontend/src/pages/public/LandingPage.tsx"
      to: "frontend/src/lib/publicApi.ts"
      via: "fetchPublicStats in useEffect"
      pattern: "fetchPublicStats"
    - from: "frontend/src/pages/public/PublicImpactPage.tsx"
      to: "frontend/src/lib/publicApi.ts"
      via: "fetchImpactSnapshots in useEffect"
      pattern: "fetchImpactSnapshots"
    - from: "frontend/src/pages/donor/DonorDashboard.tsx"
      to: "frontend/src/lib/donorPortalApi.ts"
      via: "fetchMyImpact in useEffect"
      pattern: "fetchMyImpact"
    - from: "frontend/src/pages/donor/DonorHistoryPage.tsx"
      to: "frontend/src/lib/donorPortalApi.ts"
      via: "fetchMyDonations in useEffect"
      pattern: "fetchMyDonations"
    - from: "frontend/src/components/layout/AppLayout.tsx"
      to: "frontend/src/components/ui/DarkModeToggle.tsx"
      via: "DarkModeToggle rendered in AppBar Toolbar"
      pattern: "DarkModeToggle"
    - from: "frontend/src/components/layout/AppLayout.tsx"
      to: "frontend/src/components/layout/Footer.tsx"
      via: "Footer rendered at bottom of layout Box"
      pattern: "Footer"
---

<objective>
Build all 5 page components (LandingPage, PublicImpactPage, PrivacyPolicyPage, DonorDashboard, DonorHistoryPage) and wire them into the app through routing, AppLayout navigation updates, and Footer/DarkModeToggle integration.

Purpose: Deliver the user-facing features that visitors and donors interact with. This plan consumes all the infrastructure (APIs, types, theme, components) created in Plan 01 and produces the visible application pages.

Output: Complete public-facing website (landing, impact, privacy) and donor portal (dashboard, history) with dark mode toggle in AppBar, cookie consent banner, Footer with privacy link, and role-aware navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-public-pages-donor-portal/03-RESEARCH.md
@.planning/phases/03-public-pages-donor-portal/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- these are the contracts this plan consumes -->

From frontend/src/lib/publicApi.ts:
```typescript
export const fetchPublicStats: () => Promise<PublicStats>;
export const fetchImpactSnapshots: () => Promise<ImpactSnapshot[]>;
```

From frontend/src/lib/donorPortalApi.ts:
```typescript
export const fetchMyDonations: () => Promise<DonorDonation[]>;
export const fetchMyImpact: () => Promise<DonorImpact>;
```

From frontend/src/types/PublicImpact.ts:
```typescript
export interface PublicStats {
  totalResidentsServed: number;
  totalDonationsReceived: number;
  successfulReintegrations: number;
  reintegrationRate: number;
}
export interface ImpactSnapshot {
  snapshotDate: string | null;
  headline: string | null;
  summaryText: string | null;
  month: string | null;
  avgHealthScore: number | null;
  educationProgress: number | null;
  totalResidents: number | null;
  donationsTotal: number | null;
}
```

From frontend/src/types/DonorPortal.ts:
```typescript
export interface DonorDonation {
  donationId: number; amount: number; donationType: string | null;
  donationDate: string | null; campaignName: string | null; isRecurring: boolean;
}
export interface AllocationSummary {
  safehouseName: string | null; programArea: string | null; totalAllocated: number;
}
export interface DonorImpact {
  totalDonated: number; donationCount: number;
  firstDonationDate: string | null; latestDonationDate: string | null;
  allocations: AllocationSummary[];
}
```

From frontend/src/context/ThemeContext.tsx:
```typescript
export function ThemeModeProvider({ children }: { children: ReactNode }): JSX.Element;
export function useThemeMode(): { mode: 'light' | 'dark'; toggleMode: () => void };
export function hasConsent(): boolean;
```

From frontend/src/components/ui/DarkModeToggle.tsx:
```typescript
export default function DarkModeToggle(): JSX.Element;
// Renders IconButton with sun/moon icon, calls useThemeMode().toggleMode
```

From frontend/src/components/ui/CookieConsentBanner.tsx:
```typescript
export default function CookieConsentBanner(): JSX.Element;
// Renders GDPR banner at bottom of page
```

From frontend/src/components/layout/Footer.tsx:
```typescript
export default function Footer(): JSX.Element;
// Renders footer with copyright + privacy policy link
```

From frontend/src/context/AuthContext.tsx:
```typescript
export function useAuth(): {
  authSession: AuthSession; isAuthenticated: boolean; isLoading: boolean; refreshAuth: () => Promise<void>;
}
```

From frontend/src/components/auth/ProtectedRoute.tsx:
```typescript
// Accepts role?: string prop. Redirects to /login if not authenticated, to / if wrong role.
```

Existing assets:
- frontend/src/assets/hero.png (hero background image for landing page)

Existing components available for reuse:
- MetricCard from frontend/src/components/ui/MetricCard.tsx (used in admin dashboard)
- Recharts is already installed (used in admin dashboard charts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public pages -- LandingPage, PublicImpactPage, PrivacyPolicyPage</name>
  <files>
    frontend/src/pages/public/LandingPage.tsx,
    frontend/src/pages/public/PublicImpactPage.tsx,
    frontend/src/pages/public/PrivacyPolicyPage.tsx
  </files>
  <read_first>
    frontend/src/lib/publicApi.ts,
    frontend/src/types/PublicImpact.ts,
    frontend/src/components/ui/MetricCard.tsx,
    frontend/src/pages/admin/AdminDashboard.tsx,
    frontend/src/theme.ts,
    frontend/src/assets/hero.png
  </read_first>
  <action>
    Create the `frontend/src/pages/public/` directory and build 3 page components.

    **1. LandingPage.tsx (PUB-01):**

    Sections (top to bottom):
    a) **Hero section** -- Full-width Box with `backgroundImage: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url(heroImage)` (import hero.png from '../../assets/hero.png'). backgroundSize: cover, backgroundPosition: center, white text, py: { xs: 8, md: 12 }, textAlign: center. Content: "Harbor of Hope" h2 (fontWeight 800), subtitle "Safe homes for girls who are survivors of trafficking in Central America" h5 (opacity 0.9, mb: 4), "Donate Now" Button variant="contained" size="large" with href="#donate" and sx: { px: 6, py: 1.5, fontSize: '1.1rem' }.

    b) **Mission cards section** -- Container maxWidth="lg", py: 6. Typography h4 "Our Mission" centered, mb: 4. Grid container spacing={4} with 3 Card items:
       - Card 1: icon VolunteerActivismIcon (coral), title "Safe Homes", body "Providing secure, nurturing environments for girls rescued from trafficking in Central America."
       - Card 2: icon SchoolIcon (secondary green), title "Education & Healing", body "Comprehensive programs including education, therapy, and life skills training for recovery and growth."
       - Card 3: icon PublicIcon (coral), title "Community Impact", body "Building partnerships with local communities to prevent trafficking and support survivors' reintegration."
       Each card: CardContent with icon (fontSize: 48, color primary/secondary, mb: 1), h6 title, body2 text.secondary body.

    c) **Impact stats section** -- Box with bgcolor="primary.main", color="white", py: 6. Container with Grid of 4 items (xs=6, md=3). Fetch stats via `fetchPublicStats()` in useEffect on mount. Display: totalResidentsServed ("Residents Served"), totalDonationsReceived formatted as currency ("Donations Received"), successfulReintegrations ("Successful Reintegrations"), reintegrationRate as percentage ("Reintegration Rate"). Each stat: Typography h3 (fontWeight 800) for number, body1 for label. Show CircularProgress while loading.

    d) **Donate CTA section** (id="donate") -- Container maxWidth="sm", py: 8, textAlign center. Typography h4 "Make a Difference Today", body1 explaining how to contribute (since no payment processing: "Harbor of Hope relies on the generosity of donors like you. Contact us to learn how your contribution can change lives."), Button variant="contained" with href="mailto:donate@harborofhope.org" text "Contact Us to Donate".

    **2. PublicImpactPage.tsx (PUB-02):**

    Fetch `fetchImpactSnapshots()` on mount. Show loading state with CircularProgress.

    Layout: Container maxWidth="lg", py: 4.
    - Typography h4 "Public Impact Dashboard", subtitle body1 "Anonymized data showing Harbor of Hope's impact over time."
    - **Charts section** (Grid container spacing={3}):
      a) Grid item xs=12 md=6: Paper with p: 3. Typography h6 "Average Health Score Over Time". Recharts ResponsiveContainer height={300} with LineChart. XAxis dataKey="month" (tick angle -45, textAnchor end). YAxis. CartesianGrid strokeDasharray="3 3". Tooltip. Line type="monotone" dataKey="avgHealthScore" stroke="#5B8C7A" (secondary green) name="Avg Health Score" strokeWidth={2}.
      b) Grid item xs=12 md=6: Paper with p: 3. Typography h6 "Monthly Donations". Recharts BarChart. Same axes. Bar dataKey="donationsTotal" fill="#E8735A" (coral) name="Donations ($)".
      c) Grid item xs=12 md=6: Paper with p: 3. Typography h6 "Total Residents Over Time". LineChart. Line dataKey="totalResidents" stroke="#E8735A" name="Total Residents" strokeWidth={2}.
      d) Grid item xs=12 md=6: Paper with p: 3. Typography h6 "Education Progress". LineChart. Line dataKey="educationProgress" stroke="#5B8C7A" name="Education Progress (%)" strokeWidth={2}.

    - **Recent Snapshots section**: Below charts, Typography h5 "Recent Updates". Map over first 5 snapshots, render Card for each with headline as h6, summaryText as body2, snapshotDate formatted as readable date (e.g., "March 2024").

    Filter out snapshots where all metric fields are null before charting. Sort chart data by month ascending (reverse the desc API order).

    **3. PrivacyPolicyPage.tsx (PUB-03):**

    Container maxWidth="md", py: 4. Harbor of Hope specific privacy policy (NOT generic lorem ipsum).

    Content sections (use Typography components):
    - h4 "Privacy Policy" + subtitle "Last updated: April 2026"
    - h6 "Who We Are" -- "Harbor of Hope is a nonprofit organization operating safe homes for girls who are survivors of trafficking in Central America. This policy explains how we collect, use, and protect your information when you visit our website."
    - h6 "Information We Collect" -- Bullet list (use Box with ul/li): "Account information (email, password) when you register", "Donation records if you are a registered donor", "Usage data through essential cookies required for the site to function", "Preference cookies (such as dark mode) only with your explicit consent"
    - h6 "How We Use Your Information" -- "To provide and maintain our services", "To display your donation history and impact (donors only)", "To show aggregated, anonymized impact statistics to the public", "We never sell your personal information to third parties"
    - h6 "Cookies" -- "Essential cookies: Required for authentication and site functionality. These cannot be declined.", "Preference cookies: Used to remember settings like dark mode. These are only set after you accept cookie consent.", "You can manage your cookie preferences using the consent banner shown when you first visit."
    - h6 "Data Security" -- "We use industry-standard security measures including HTTPS encryption, secure authentication, and role-based access controls to protect your data."
    - h6 "Your Rights" -- "You may request access to, correction of, or deletion of your personal data by contacting us."
    - h6 "Contact Us" -- "For privacy-related inquiries, email us at privacy@harborofhope.org"
  </action>
  <verify>
    <automated>cd "/Users/waylansmac/INTEX 2026/frontend" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - LandingPage renders hero with background image, 3 mission cards, 4 live impact stats from API, and donate CTA
    - PublicImpactPage renders 4 Recharts charts (health scores, donations, residents, education) from impact snapshots API
    - PrivacyPolicyPage renders Harbor of Hope specific privacy policy with all required sections (cookies, data use, rights)
    - All pages use MUI components and theme colors (coral/cream/Nunito)
    - No PII displayed on any public page
    - TypeScript compiles with no errors
  </done>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with 0 errors
    - LandingPage.tsx imports and calls fetchPublicStats
    - LandingPage.tsx imports hero.png from assets
    - LandingPage.tsx contains "Donate Now" button text
    - PublicImpactPage.tsx imports and calls fetchImpactSnapshots
    - PublicImpactPage.tsx imports LineChart and BarChart from recharts
    - PrivacyPolicyPage.tsx contains "Harbor of Hope" (not generic placeholder)
    - PrivacyPolicyPage.tsx explains cookie types (essential vs preference)
    - None of the 3 pages import or reference any user name, email, or PII fields
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Donor portal pages, AppLayout update, and App.tsx routing</name>
  <files>
    frontend/src/pages/donor/DonorDashboard.tsx,
    frontend/src/pages/donor/DonorHistoryPage.tsx,
    frontend/src/components/layout/AppLayout.tsx,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/lib/donorPortalApi.ts,
    frontend/src/types/DonorPortal.ts,
    frontend/src/components/layout/AppLayout.tsx,
    frontend/src/components/layout/Footer.tsx,
    frontend/src/components/ui/DarkModeToggle.tsx,
    frontend/src/components/ui/CookieConsentBanner.tsx,
    frontend/src/App.tsx,
    frontend/src/context/AuthContext.tsx,
    frontend/src/components/auth/ProtectedRoute.tsx,
    frontend/src/components/ui/MetricCard.tsx
  </read_first>
  <action>
    Create the `frontend/src/pages/donor/` directory and build 2 donor pages, then update AppLayout and App.tsx.

    **1. DonorDashboard.tsx (PORTAL-02):**

    Fetch `fetchMyImpact()` on mount. Loading state with CircularProgress.

    Layout: Container maxWidth="lg", py: 4.
    - Typography h4 "Your Impact", subtitle body1 color="text.secondary" "See how your contributions are making a difference."

    - **Summary cards** (Grid container spacing={3}, mb: 4):
      - Grid item xs=12 sm=6 md=3: Card with CardContent showing totalDonated formatted as currency (Typography h4 fontWeight 700) with label "Total Donated" (body2 text.secondary). Use the coral color for the number.
      - Grid item xs=12 sm=6 md=3: Card with donationCount (Typography h4) and label "Donations Made"
      - Grid item xs=12 sm=6 md=3: Card with firstDonationDate formatted as readable date and label "First Donation"
      - Grid item xs=12 sm=6 md=3: Card with latestDonationDate formatted as readable date and label "Latest Donation"

    - **Allocation breakdown** (below cards):
      Typography h5 "Where Your Donations Go", mb: 2.
      If allocations.length > 0: MUI Table (TableContainer + Paper) with columns: Safehouse, Program Area, Amount Allocated (formatted as currency). One row per allocation.
      If allocations.length === 0: Typography body1 text.secondary "No allocation data available yet."

    - **Navigation link**: Button variant="outlined" component={RouterLink} to="/donor/donations" text "View Full Donation History" with ArrowForwardIcon endIcon.

    **2. DonorHistoryPage.tsx (PORTAL-01):**

    Fetch `fetchMyDonations()` on mount. Loading state with CircularProgress.

    Layout: Container maxWidth="lg", py: 4.
    - Typography h4 "Your Donation History"

    - **Donations table**: TableContainer + Paper with Table:
      - Columns: Date, Type, Campaign, Amount, Recurring
      - For each donation: format donationDate as readable date, show donationType, campaignName (or "-" if null), amount as currency, isRecurring as Chip label "Yes"/"No" with color success/default.
      - If donations.length === 0: Box with Typography body1 text.secondary "No donations found for your account."

    - **Back link**: Button variant="text" component={RouterLink} to="/donor/dashboard" text "Back to Dashboard" with ArrowBackIcon startIcon.

    **3. AppLayout.tsx updates (PUB-03, PUB-04, PUB-05):**

    Read the existing AppLayout.tsx (already loaded via read_first) and modify:

    a) **Add imports**: Import DarkModeToggle from '../ui/DarkModeToggle', Footer from './Footer', CookieConsentBanner from '../ui/CookieConsentBanner'. Import `useAuth` is already imported.

    b) **Role-aware navigation** in the Toolbar:
       - Get `authSession` from `useAuth()` (already destructured, just add authSession)
       - Derive: `const isAdmin = authSession.roles.includes('Admin');` and `const isDonor = authSession.roles.includes('Donor');`
       - Nav buttons update:
         - Always show: "Home" (to "/"), "Impact" (to "/impact") -- these are public
         - If not authenticated: "Login" (to "/login"), "Register" (to "/register")
         - If isAdmin: "Dashboard" (to "/admin/dashboard") -- admin sidebar handles rest
         - If isDonor (and not admin): "My Dashboard" (to "/donor/dashboard"), "My Donations" (to "/donor/donations")
         - If authenticated: "MFA Settings" (to "/manage-mfa"), "Logout" (to "/logout")
       - Add DarkModeToggle component in the Toolbar, before the nav buttons (after the flex-grow title, before the button group). Or after the button group -- either works. Place it as: `<DarkModeToggle />` inside the Toolbar.

    c) **Footer placement**: Add `<Footer />` at the very bottom of the outermost Box (after both the admin branch and the public branch of the conditional rendering). It should appear on ALL pages (admin and public). Place it after the closing of the conditional `{isAdminRoute ? (...) : (...)}` block but before the closing `</Box>`.

    d) **Cookie consent banner**: Add `<CookieConsentBanner />` at the very bottom of the component return, after Footer, before closing `</Box>`. The banner renders fixed at the bottom of the viewport via react-cookie-consent's built-in positioning.

    e) **Donor route detection**: Add `const isDonorRoute = location.pathname.startsWith('/donor');` alongside the existing `isAdminRoute`. Public pages and donor pages should NOT show the AdminSidebar. The admin branch condition stays `isAdminRoute` (do not show sidebar for donor routes).

    **4. App.tsx routing updates:**

    a) **Add imports** for all new pages:
       ```typescript
       import LandingPage from './pages/public/LandingPage';
       import PublicImpactPage from './pages/public/PublicImpactPage';
       import PrivacyPolicyPage from './pages/public/PrivacyPolicyPage';
       import DonorDashboard from './pages/donor/DonorDashboard';
       import DonorHistoryPage from './pages/donor/DonorHistoryPage';
       ```

    b) **Replace the placeholder "/" route** (currently renders a Container with "Harbor of Hope" text) with `<LandingPage />`.

    c) **Add public routes** (inside the AppLayout Route, alongside /login and /register):
       ```
       <Route path="/impact" element={<PublicImpactPage />} />
       <Route path="/privacy" element={<PrivacyPolicyPage />} />
       ```

    d) **Replace donor dashboard placeholder** (currently at /donor/dashboard showing "Coming in Phase 3"):
       ```
       <Route path="/donor/dashboard" element={
         <ProtectedRoute role="Donor"><DonorDashboard /></ProtectedRoute>
       } />
       ```

    e) **Add donor history route**:
       ```
       <Route path="/donor/donations" element={
         <ProtectedRoute role="Donor"><DonorHistoryPage /></ProtectedRoute>
       } />
       ```

    f) **Remove old imports** that are no longer needed: Remove `Typography` and `Container` from MUI imports if they were only used by the old placeholder "/" route. Check if they are still used elsewhere in App.tsx before removing.
  </action>
  <verify>
    <automated>cd "/Users/waylansmac/INTEX 2026/frontend" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - DonorDashboard shows impact summary (total donated, count, dates, allocation table)
    - DonorHistoryPage shows donation history table with date, type, campaign, amount, recurring
    - AppLayout has DarkModeToggle in AppBar, Footer at bottom of all pages, CookieConsentBanner
    - AppLayout shows role-aware nav: public links for visitors, admin links for admins, donor links for donors
    - App.tsx routes "/" to LandingPage, "/impact" to PublicImpactPage, "/privacy" to PrivacyPolicyPage
    - App.tsx routes "/donor/dashboard" to DonorDashboard and "/donor/donations" to DonorHistoryPage (both Donor-protected)
    - All "Coming in Phase 3" placeholders are replaced with real components
    - TypeScript compiles with no errors
  </done>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with 0 errors
    - App.tsx imports LandingPage and uses it for "/" route (no more placeholder text)
    - App.tsx has routes for /impact, /privacy, /donor/dashboard, /donor/donations
    - DonorDashboard.tsx calls fetchMyImpact and renders allocation table
    - DonorHistoryPage.tsx calls fetchMyDonations and renders table rows
    - AppLayout.tsx imports and renders DarkModeToggle, Footer, CookieConsentBanner
    - AppLayout.tsx shows "My Dashboard" link for Donor role users
    - AppLayout.tsx does NOT show AdminSidebar for donor routes
    - Footer appears on both admin and public pages (rendered outside conditional branch)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 3 requirements</name>
  <files>none</files>
  <action>
    Human verifies all 7 requirements (PUB-01 through PUB-05, PORTAL-01, PORTAL-02) are working correctly in the browser. Start backend and frontend dev servers, then walk through each requirement.
  </action>
  <verify>Human confirms all requirements pass visual and functional testing</verify>
  <done>All 7 Phase 3 requirements verified by human in browser</done>
  <what-built>Complete public pages (landing, impact, privacy) and donor portal (dashboard, history) with dark mode, cookie consent, and role-aware navigation</what-built>
  <how-to-verify>
    1. Start backend: `cd backend/HarborOfHope.API && dotnet run`
    2. Start frontend: `cd frontend && npm run dev`
    3. Open http://localhost:5173/ in browser

    **PUB-01 -- Landing Page:**
    - Verify hero section with background image, "Harbor of Hope" title, subtitle, "Donate Now" button
    - Verify 3 mission cards below hero (Safe Homes, Education and Healing, Community Impact)
    - Verify impact stats bar (coral background) showing live numbers from database
    - Verify donate CTA section at bottom

    **PUB-02 -- Public Impact Dashboard:**
    - Click "Impact" in navigation bar
    - Verify 4 charts render with real data (health scores, donations, residents, education)
    - Verify recent updates section below charts

    **PUB-03 -- Privacy Policy:**
    - Scroll down to footer on any page, click "Privacy Policy" link
    - Verify policy text mentions "Harbor of Hope" specifically
    - Verify sections: Who We Are, Information We Collect, Cookies, etc.

    **PUB-04 -- Cookie Consent:**
    - Open DevTools > Application > Cookies, clear all cookies for localhost
    - Refresh page -- verify cookie consent banner appears at bottom
    - Click "Decline" -- verify no "darkMode" cookie appears in DevTools
    - Toggle dark mode -- verify theme changes visually but NO darkMode cookie is set
    - Refresh page -- verify banner reappears (consent not stored on decline)
    - Click "Accept" -- verify harborCookieConsent cookie is set
    - Toggle dark mode -- verify darkMode cookie now appears in DevTools
    - Refresh page -- dark mode should persist

    **PUB-05 -- Dark Mode:**
    - Click the dark mode toggle (moon/sun icon) in the AppBar
    - Verify background changes from cream to dark gray, text from dark to light
    - Verify cards, tables, and charts adapt to dark theme
    - Accept cookie consent, toggle dark mode, refresh -- should persist

    **PORTAL-01 -- Donor Donation History:**
    - Log in as donor@harbor.local (password from test accounts)
    - Click "My Donations" in nav
    - Verify table shows donations belonging to this donor only (not all donations in system)

    **PORTAL-02 -- Donor Impact Summary:**
    - Click "My Dashboard" in nav
    - Verify impact cards (Total Donated, Donations Made, First/Latest Donation dates)
    - Verify allocation breakdown table (if allocations exist for this donor)

    **Cross-cutting:**
    - Verify footer appears on all pages (public, admin, donor)
    - Verify admin user does NOT see donor nav links
    - Verify donor user does NOT see admin sidebar
    - Verify unauthenticated user sees Login/Register but not Dashboard links
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd frontend && npx tsc --noEmit` -- 0 errors
2. Backend build: `cd backend/HarborOfHope.API && dotnet build` -- 0 errors
3. Visual verification of all 7 requirements via browser testing (checkpoint task above)
4. Cookie consent functional test: decline consent, toggle dark mode, check no cookie set
5. Donor data isolation: log in as donor, verify only own donations visible
</verification>

<success_criteria>
- All 5 pages render without errors and display real data from the backend
- Landing page has hero, mission cards, live stats, and donate CTA
- Public impact dashboard shows 4 charts with Recharts using anonymized data
- Privacy policy is Harbor of Hope specific (not generic)
- Cookie consent banner appears and functionally gates the darkMode cookie
- Dark mode toggle works and persists via cookie after consent
- Donor can see their own donation history and impact summary (not other donors' data)
- Footer with privacy policy link appears on all pages
- Role-aware navigation shows correct links for admin, donor, and visitor users
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-pages-donor-portal/03-02-SUMMARY.md`
</output>
