---
phase: 06-polish-final-deploy
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04]

must_haves:
  truths:
    - "Running deploy.sh successfully provisions all Azure resources and deploys code"
    - ".NET App Service serves the React SPA at root URL and API endpoints at /api/*"
    - "Flask App Service responds to /health with list of loaded models"
    - ".NET backend can reach Flask API and return ML predictions"
    - "PostgreSQL database is seeded with all 17 tables on first startup"
    - "Site is publicly accessible via https://{app-name}.azurewebsites.net"
    - "Three test accounts work: admin, donor, MFA-enabled"
  artifacts:
    - path: "azure-deploy/deploy.sh"
      provides: "Executed deployment script"
  key_links:
    - from: "https://{app-name}.azurewebsites.net"
      to: "Azure App Service (.NET)"
      via: "HTTPS public URL"
      pattern: "azurewebsites.net"
    - from: ".NET App Service"
      to: "Flask App Service"
      via: "HttpClient with MlApiUrl config"
      pattern: "MlApiUrl"
    - from: ".NET App Service"
      to: "Azure Database for PostgreSQL"
      via: "DefaultConnection connection string"
      pattern: "DefaultConnection"
---

<objective>
Execute the Azure deployment script to deploy all three services, verify the deployed site works end-to-end, and confirm test accounts are functional.

Purpose: This is the final deployment execution. Plan 01 polished the frontend, Plan 02 prepared the deployment infrastructure. This plan runs the deployment and verifies everything works in production.
Output: A publicly accessible site at an Azure URL with all services connected and test accounts ready for grading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-final-deploy/06-01-SUMMARY.md
@.planning/phases/06-polish-final-deploy/06-02-SUMMARY.md
@azure-deploy/deploy.sh
@azure-deploy/README-DEPLOY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Azure deployment script and verify services</name>
  <files></files>
  <action>
Run the deployment script from the project root. The user must have already completed the Azure login checkpoint from Plan 02.

1. Prompt the user for a database admin password (must be complex: 8+ chars, uppercase, lowercase, digit, special char for Azure PostgreSQL requirements).

2. Execute the deployment:
```bash
cd /Users/waylansmac/INTEX\ 2026
DB_ADMIN_PASSWORD="<user-provided>" bash azure-deploy/deploy.sh
```

3. Monitor output for errors. Common issues:
   - **SKU not available in region**: Try `--location westus2` instead of `eastus`
   - **Name already taken**: Append a unique suffix to APP_NAME (e.g., `harborofhope-2026`)
   - **Quota exceeded**: Switch to Free tier (`--sku F1`) instead of Basic
   - **Build failure**: Check that `npm ci` and `dotnet publish` succeed locally first

4. Capture the output URLs (main site URL and Flask API health endpoint).

5. Wait 2-3 minutes for Azure to finish provisioning and starting the apps.

6. Verify Flask API health:
```bash
curl -s https://{flask-app-name}.azurewebsites.net/health | python3 -m json.tool
```
Expected: `{"status": "ok", "models_loaded": ["donor-churn", "social-media", ...], "models_missing": []}`

7. Verify .NET backend API:
```bash
curl -s https://{app-name}.azurewebsites.net/api/public/stats | python3 -m json.tool
```
Expected: JSON with public stats (totalResidentsServed, totalDonations, etc.)

8. Verify SPA serves at root:
```bash
curl -s -o /dev/null -w "%{http_code}" https://{app-name}.azurewebsites.net/
```
Expected: 200

If any step fails, check logs:
```bash
az webapp log tail --resource-group harborofhope-rg --name {app-name}
az webapp log tail --resource-group harborofhope-rg --name {flask-app-name}
```

Troubleshoot and re-deploy the specific failing service as needed.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://harborofhope.azurewebsites.net/ 2>/dev/null || echo "Deployment URL not yet available -- verify manually after deploy"</automated>
  </verify>
  <done>
    - deploy.sh completed without errors
    - Flask /health returns status ok with all 8 models loaded
    - .NET /api/public/stats returns valid JSON
    - Root URL serves React SPA (HTTP 200 with HTML content)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of deployed site</name>
  <files></files>
  <action>
Full Azure deployment of all three services has been completed: React SPA + .NET API (single App Service), Flask ML API (second App Service), PostgreSQL database (Azure Database for PostgreSQL). The site should be live at the Azure URL.

Verification steps for the user:
1. Open the deployed URL in browser (https://{app-name}.azurewebsites.net)
2. Verify the landing page loads with hero image, mission cards, and impact stats
3. Click "Impact" to check the public impact dashboard shows aggregated data
4. Log in with admin account (admin@harborofhope.org / the seeded password)
   - Verify admin dashboard loads with metric cards
   - Navigate to Residents page to verify data table loads with resident records
   - Navigate to Donors page to verify supporters list loads
5. Log out, then log in with donor account (donor@harborofhope.org / seeded password)
   - Verify donor dashboard shows their donation history
6. On mobile (or browser DevTools at 375px width):
   - Verify hamburger menu appears
   - Verify tables scroll horizontally
7. Run Lighthouse accessibility audit on the landing page -- score should be >= 90
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://harborofhope.azurewebsites.net/ 2>/dev/null || echo "Check URL manually"</automated>
  </verify>
  <done>
    - Landing page loads at public Azure URL
    - Admin login works and dashboard displays data
    - Donor login works and shows donation history
    - Mobile responsive (hamburger menu, scrollable tables)
    - Lighthouse accessibility score >= 90%
    - User has confirmed "approved"
  </done>
</task>

</tasks>

<verification>
1. Site accessible at public Azure URL (DEPLOY-04)
2. .NET backend serves React SPA and API endpoints (DEPLOY-01)
3. PostgreSQL database on Azure with seeded data (DEPLOY-02)
4. Flask ML API accessible from .NET backend (DEPLOY-03)
5. Test accounts functional (admin, donor, MFA)
6. Responsive design works on deployed site (A11Y-02, A11Y-03, A11Y-04)
7. Lighthouse accessibility >= 90% (A11Y-01)
</verification>

<success_criteria>
- All three Azure services are running and connected
- Site is publicly accessible via HTTPS URL
- Landing page loads, admin dashboard works, donor portal works
- ML predictions are functional (churn badges visible on donors page)
- Mobile responsive (hamburger menu, scrollable tables)
- Lighthouse accessibility score >= 90%
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-final-deploy/06-03-SUMMARY.md`
</output>
